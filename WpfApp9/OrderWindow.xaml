<Window x:Class="WpfApp9.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp9"
        mc:Ignorable="d"
        Title="Заказы" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" Margin="10">
            <ItemsControl x:Name="OrderList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock FontWeight="SemiBold">
                                    <Run Text="{Binding ProductArticleText, StringFormat='Артикул:{0}'}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding StatusText, StringFormat = 'Cтатус заказа{0}'}"/>
                                <TextBlock Text="{Binding AddressText}"/>
                                <TextBlock Text="{Binding CreationDate}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="10">
                                <TextBlock Text="Дата доставки"/>
                                <TextBlock Text="{Binding DeliveryDate}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Button x:Name="ExitButton" Content="Выйти" HorizontalAlignment="Left" Grid.Row="1" Click="ExitButton_Click"/>

    </Grid>
</Window>
